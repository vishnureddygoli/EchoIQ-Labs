[project]
name = "echoiq-worker"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["psycopg[binary]>=3.2", "redis>=5.0", "pydantic-settings>=2.3"]

[tool.setuptools.packages.find]
where = ["."]
include = ["app*"]
